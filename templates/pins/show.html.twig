{% extends 'base.html.twig' %}

{% block title pin.title %}

{% block body %}
    <article>
        <h2> {{ pin.title }}</h2>
        <p>{{ pin.description }}</p>
    </article>
    <a href="{{ path('app_home') }}">Go Back</a>
{% endblock %}
